import 'package:flutter_test/flutter_test.dart';
import 'package:la_piramide/main.dart';

void main() {
  test('PyramidApp is available as the mobile entry point', () {
    expect(const PyramidApp(), isA<PyramidApp>());
  });
}
